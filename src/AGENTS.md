# Vault Agent Entry

For a known project or file, load its relevant instructions and source documents directly. Read `00-AI/START-HERE.md` when vault routing is needed; load only the governance rules and templates required by the task.

Do not treat this pointer as a second rule source. The maintained workflow lives under `00-AI/`.
